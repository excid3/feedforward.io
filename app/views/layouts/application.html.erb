<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Feed Forward" %></title>
    <%= csrf_meta_tags %>
    <%= auto_discovery_link_tag(:rss, links_url(format: "rss")) %>
    <%= favicon_link_tag "http://feedforward.herokuapp.com/assets/favicon.png" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "Feed Forward", root_url, class: "brand" %>
          <div class="container nav-collapse">
            <ul class="nav pull-right">
              <li><%= link_to "http://feeds.feedburner.com/FeedForward" do %>RSS Feed <i class="icon-rss"></i><% end %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="span12">

          <% if notice %>
            <div class="alert alert-info">
              <button type="button" class="close" data-dismiss="alert">×</button>
              <%= notice %>
            </div>
          <% end %>

          <% if alert %>
            <div class="alert alert-error">
              <button type="button" class="close" data-dismiss="alert">×</button>
              <%= alert %>
            </div>
          <% end %>

          <%= yield %>
        </div>
      </div><!--/row-->

      <hr/>

      <footer>
        <div class='pull-right'>
          <small>
            <% if user_signed_in? %>
              Logged in as <%= current_user.email %> |
              <%= link_to "Logout", destroy_user_session_path, method: :delete %>
            <% else %>
              <%= link_to "Login", new_user_session_path %>
            <% end %>
          </small>
        </div>
        <p><small>
          Made by <%= link_to "Chris Oliver", "http://excid3.com", target: "_blank" %> |
          <%= link_to "@excid3", "https://twitter.com/excid3", target: "_blank" %>
        </small></p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
